{
  "name": "napcat.native",
  "version": "1.0.0",
  "description": "NapCat.Native",
  "scripts": {
    "build": "npx node-gyp build",
    "rebuild": "npx node-gyp rebuild",
    "configure": "npx node-gyp configure"
  },
  "dependencies": {
    "node-gyp": "^10.1.0"
  }
}